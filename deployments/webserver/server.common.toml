specVersion = 1
kind = "sdkwork.webserver.server"
id = "codebox"
description = "sdkwork-codebox web server (application.public-ingress)"

[nginx]
enabled = true
profile = "http-core-v1"
unknownDirectivePolicy = "error"
strict = true
confFile = "nginx.conf"

[main]
user = "sdkwork"
workerProcesses = "auto"
pid = "/run/sdkwork/codebox/webserver.pid"
errorLog = "/var/log/sdkwork/codebox/webserver/error.log warn"

[main.events]
workerConnections = 1024

[http]
sendfile = true
keepaliveTimeout = 75
clientMaxBodySize = "1100m"
serverTokens = "off"
gzip = true

[[http.upstream]]
name = "gateway"
loadBalancing = "least-connections"
keepalive = 32

